# 时音更新日志

## v3.0.8

### 新功能
- **歌曲信息查看（移动端/车机/PC）**：新增歌曲信息——专辑名、发行时间、专辑简介、语种经 `GET /album/detail` 拉取，词曲作者从歌词正文提取（无则隐藏该行）。移动端在歌曲详情抽屉与详情菜单/车机菜单进入，PC 新增歌曲详情页（评论/详情双 tab），底栏歌名进详情、评论按钮直达评论
- **歌词进度调整**：歌词偏移可手动微调，入口覆盖详情弹层「歌词进度」、长按歌词行、PC 封面按钮与歌词右键、桌面歌词悬浮窗快捷菜单；偏移**按歌曲记忆**持久化，播放页歌词定位与桌面歌词共用同一偏移
- **应用日志落盘**：新增日志服务接管调试输出（Android 应用专属外部存储 `logs/app.log`、桌面文档目录），2000 行环形缓冲 + 512KB 轮转 `.old`，用户侧偶发问题事后可取证

### 修复
- **桌面歌词位置记忆**：全新安装用户首次拖动/关闭悬浮窗后，第二次启动歌词整体上跳 248px；从 v3.0.7 升级的存量位置少补 40px（菜单带 172→212）导致下沉；每会话第一次拖动的落盘被抑制开关吞掉。三处一并修复：位置语义由写值方随存随置，存量迁移提前到子窗创建之前
- **曲末不跳下一首三层加固**：曲末停滞 watchdog 到点后按 1.5s×3 复检，仍冻结才强制按播完推进（用户暂停在曲尾永不误切）；通知栏/锁屏/耳机播放键在曲末先 `pause + seek(0)` 再起播，重播后再播完照样自动切歌；代理上游加响应头 15s / 数据流空闲 20s 超时，CDN 断供从无限挂起变响亮失败并交给自愈链
- **移动端歌词进度面板**：打开面板不再连带退出播放页；矮窗口可滚动，底部不再溢出
- **横屏/车机更多菜单**：桌面歌词项点一次不再退出播放页
- **PC 锚定歌词进度面板**：状态行恒定占位，高度不再随首次调整跳变；副标题跟随实时曲目
- **歌曲详情评论 tab**：加 keep-alive，标题计数经会话缓存解耦懒构建

### 优化
- **关于页更新日志**：只展示 v2.4.0 及更新的较新版本（27 → 17 个版本卡片），不再一路铺到 v1.0.0；完整历史仍随仓库发布
- **移动端详情菜单抽屉**：标题/宫格钮/列表行字号下调一档、纵向间距同步收窄，去掉适老化大字观感
- **歌词偏移落盘**：首写立即 + 500ms 窗口合并，长按连调 150 次由 150 次写盘降到 2 次
- 歌词进度样式对齐参考稿（秒表图标、已调偏移点亮宫格、短读数「0 秒/+0.5 秒」）；歌曲详情弹层换肤为浅蓝灰底 + 白色悬浮宫格钮
- 系统媒体键重播容差 250ms→1s；落盘日志单条超 16KB 截断，桌面退出前与移动端 detached 时补刷

## v3.0.7

> v3.0.6 因问题撤回未正式发布，其内容已并入本版。

### 修复
- **桌面歌词回桌面伴奏期空窗（Android）**：切前台不再销毁服务实例，回桌面用缓存即刻重建并恢复逐字进度，show 自带歌词、隐藏期只写缓存不建窗
- **桌面歌词关闭后复活/进度重放**：补建窗口增加意图门控，用户点 X 后补显式关闭清缓存；同句重复推送不再重置高亮，快照与当前句不一致时不恢复旧进度
- **桌面歌词杂项加固**：isVisible 改真实展示态、前台服务失败记日志、颜色低 alpha 不再回白、停服改为批次静默后确认、瞬时失焦（下拉栏/权限弹窗）不再误弹、切歌即清旧缓存、FGS 类型与后台启动兜底、默认配色字号对齐设置页
- **通知/红心/下载对账**：通知红心冷启动门控对齐 UI，红心失败透出可读文案并正确回滚，下载跨盘搬移残片清理与不可见目录误删索引修复，更新弹窗下载区截断精确匹配
- **弹窗与列表**：确认弹窗内置连点守卫，我的页多选删除、登录错误弹窗统一应用语言，播放队列歌名跑马灯补齐全行口径，桌面大家都在听行高回退
- **桌面歌词默认外观**：移动端恢复默认改为双行显示 + 50% 背景，桌面默认双行两端对齐（新安装生效，已有设置不受影响，可点恢复默认一键还原）
- **PC 端页面布局被车机开关劫持**：电脑上残留的移动端「车机模式」开关不再让推荐/排行/电台/搜索等页面变成车机/手机布局（该开关本就不在桌面展示，桌面读取恒为关闭），通知栏红心按钮同步恢复显示；手机/车机行为不变
- **车机/分栏歌词切歌卡死**：横屏分栏歌词面板自监听播放器状态，切歌后歌词到达与清空、空歌词时「正在准备音乐...」→「暂无歌词」翻转均自刷新，不再依赖播放页父级重建透传，面板复用场景不再卡死
- **搜索结果歌名**：单曲搜索优先使用酷狗原始歌名（OriSongName + 版本后缀），歌手前缀不再依赖启发式剥离，误留/误剥两类问题一并解决；字段缺失时回退备选字段链并保留原剥离兜底

### 优化
- **内存与性能**：首页桌面分支取消 eager 构建改懒加载，图片缓存收敛 300 张/128MB，保活栈 LRU 上限 3，桌面网格封面解码降档，Windows libmpv 解复用缓存 32MB→8MB
- **移动端设置页**：桌面歌词设置页隐藏 PC 专属对齐项，锁定/穿透开关说明对齐移动端行为

## v3.0.5

### 新功能
- **Windows 单实例**：重复启动不再开出第二个窗口，而是唤醒已有窗口并置前；托盘左键恒为恢复并置前
- **播放页保持屏幕常亮开关（Android）**：设置 → 播放，可关闭进播放页强制常亮；开启时也仅在播放中常亮，暂停/播完即交回系统休眠
- **歌名跑马灯铺开**：长歌名不再被截断，歌单/搜索/本地/已下载/收藏各列表、首页横轨、移动端迷你播放栏统一滚动显示；搜索框失焦跑马灯不再盖住清除按钮

### 修复
- **冷启动幽灵桌面歌词通知**：冷启动后通知栏不再凭空出现"桌面歌词显示中"；未加载媒体时的空媒体通知不再广播
- **通知栏红心/桌面歌词按钮状态同步**：切歌当刻即刷新图标，回调异常只记日志不再破坏通知刷新
- **取消红心不生效**：持久化 hash→fileId 映射（重启后不再误删），无快照/快照过期自动同步重试，服务端失败回滚本地态并提示
- **专辑搜索歌手名恒为空**：专辑搜索返回体的歌手字段（`singer`）纳入解析，专辑 tab 恢复显示歌手
- **创建歌单弹窗**：居中 dialog 新版式（浅底输入框 + 为空禁用创建），键盘弹起不再把按钮挤出卡外，误触遮罩不再丢失输入；删除/清空确认弹窗统一同语言
- **播放历史顶栏穿透**：标题栏加不透明底，歌曲上滑不再从标题下透出
- **登录页验证码框描边**：补齐各状态边框，聚焦时不再浮出第二圈描边；点热搜词/切平台/切类型后自动收键盘
- **PC 自定义下载目录**：换目录/恢复默认时旧目录纳入历史对账，跨盘搬迁自动复制后删源，保存前先探测可写、不可写当场报错不再静默存错
- **起播超时误清失败计数**：超时但引擎实际在播不再跳过成功记账，自动切歌统计更准
- **流式响度分析中断污染**：中断的半截结果不再写入响度缓存，弱网切歌的均衡增益更准；本地分析落盘原子化，跳过 0 字节残留

### 优化
- **自定义音效页现代化**：胶囊分段切换、收紧的频段滑块与曲线区、双药丸预设按钮，与应用 UI 语言统一
- **移动端列表视觉统一**：歌曲行收敛为公共组件，歌单行去卡片化改扁平紧凑，搜索框统一为胶囊样式
- **播放页性能**：封面旋转背景图层换序消除逐帧全屏重模糊，划走的页面停动画不再空转
- **Linux 安装包**：deb 运行期依赖补上 `libnotify4`，通知开箱可用

## v3.0.4

### 修复
- **手机播完不自动跳下一首（严重）**：Android 原生的 `play(Result)` 只在曲目走到 EOF 时才回执（just_audio `AudioPlayer.java:971-986`），而每次切歌前都会先暂停旧歌——「起播确认」因此挂到曲末，把播放器内部的「换源中」深度整整锁住一首歌，曲末的 completed 事件被整条挡掉；3.0.3 又把曲末兜底推进的存活判据换成曲末必然为 false 的 `isPlaying`，两条路同时失效，队列永久停在当前曲（进度停在曲尾、标题不变）。现将起播确认改为有界等待（2 秒，声音不受影响），completed 主分支恢复自主推进；曲末兜底也不再以 `isPlaying` 判活，作为第二道保险保留
- **播放中途断流/解码失败被静默吞掉**：同一个「换源深度锁住」还让中途错误处理在整首歌期间失效——界面显示在播放、实际没有声音、不报错也不自动跳过。随上一条一并修复
- **同一首歌重播后不再自动切歌**：曲末点播放（或曲末点歌词定位）会原地重新起播且不经过完整加载流程，上一轮遗留的「完成去重标记」把这一遍播完的 completed 事件吞掉；现手动起播会清掉该标记与曲末失败残留
- **曲末坏帧自动切歌的预算会永久失效**：60 秒墙钟预算只在「自然播完」时清零，而曲末失败这条路本来就不会再走自然播完——一首歌之后就永久判为超预算、停止切歌并停在曲尾。现墙钟每次起播成功重新起算，跨曲坏尾仍由次数上限拦住
- **单曲循环只循环一次、切音质后收尾卡住**：同为「起播确认」无界等待导致，一并修复

### 说明
- 本版本为 **Android 端播放可靠性修复**，PC 端不受影响（桌面适配层的 `play()` 立即回执，不存在该问题）

## v3.0.3

### 修复
- **PC 自动切到下一首播不出声**：桌面音频适配层此前从不在 EOF 回写播放态，`just_audio` 的 `playing` 在曲末恒为 true 导致重播被短路空转（UI 显示播放中但无声）。现 EOF 回写播放态，自动切歌 / 曲末点播放 / 单曲循环重播恢复正常
- **PC 曲末偶发打断下一首**：EOF 后的 ffmpeg 连接清理错误曾被抬成致命错误，中断在途的下一首加载；现 completed 之后的错误只记日志
- **曲末坏帧误弹「暂无可播放音源」**：CDN/FLAC 最后一两帧损坏（1.5s 内）按播完自动切下一首，不再打扰；连续坏尾有次数 + 墙钟双预算防无限静默跳歌，耗尽后停住并持久提示
- **曲末点歌词/高潮试听定位被冲掉**：统一走不碰位置的起播收敛，不再 seek 回零；高潮试听到点自动暂停不再误把已停播放拉起

### 说明
- 本版本以播放器修复为主；移动端同步受益（曲末错误提示收敛、切网误重播等），无其他行为变化

### 新功能
- **PC 自定义下载位置**：设置 → 桌面 → 下载位置，可自选歌曲保存目录并一键恢复默认；已有下载不受影响，下次对账自动搬入新目录
- **Android 32 位安装包**：新增 `skia-arm32` 变体，老车机 / 32 位设备可正常安装；应用内更新按设备架构自动选对应 APK（arm32 车机不会再误下 64 位包装不上）
- **Release 页产物下载指引**：发布说明新增「📥 下载」区，按平台列出全部产物及适用人群（手机 / 老机器 / 车机 / PC），每个产物附带 SHA256 校验文件；应用内更新弹窗只展示更新内容，不刷屏

### 修复
- **PC「打开文件夹」错开到文档目录**：下载的歌曲名含空格时，explorer 定位参数被整体加引号解析失败，explorer 退回打开默认位置；现改经 PowerShell 原样传递，正确打开所在文件夹并选中文件
- **PC 点击「下载位置」偶发失败**：file_picker 的 Windows 弹窗组件偶发异常（上游已知问题），现自动降级到系统备用选目录弹窗，失败不再静默、提示携带真实原因
- **平板形态窗口宽度跨过阈值时崩溃**（`ScrollController attached to multiple scroll views`）：首页滚动子树跨形态整树搬运，滚动位置与播放状态全程保留

### 优化（移动端 / 平板）
- **平板形态重设计**：宽屏自动切换为触屏侧栏（推荐 / 排行榜 / 电台 / 我的 + 搜索入口 + 迷你播放器停靠底部），内容区网格化、搜索栏与卡片限宽，告别窄轨大留白的拉伸布局；手机、车机、桌面形态不受影响
- **车机搜索页**：听歌识曲入口移至搜索框左侧图标（对齐移动端），不再紧贴「搜索」按钮造成误触

## v3.0.1

### 修复（PC 桌面端）
- **响度均衡增益 3 倍失真**：PC 音频引擎（mpv）的音量是立方刻度，适配层此前按线性透传，导致所有响度增益被放大 3 倍——轻歌炸响削波、响歌过度发闷（如《月半小夜曲》应 +4.9dB 实际 +14.6dB）。现按立方刻度精确换算，不同歌曲终于在同一响度上；音量滑杆手感（下半段几乎无声）一并恢复
- **切歌瞬间音量忽大忽小**：缓存未命中的新歌此前会带着上一首的响度增益播放到分析完成，现在先平滑回到原始音量、分析完成后再过渡到正确增益（全平台生效）

### 优化（PC 桌面端）
- **响度分析流式化**：网络歌曲不再整曲下载后才开始分析，改为边下边解（与手机一致），弱网下约 0.5 秒即有初步均衡效果，且不再产生临时文件
- Rust 代码全量格式化并清零 clippy 告警

### 说明
- 本版本以 PC 桌面端修复为主；移动端仅包含切歌响度过渡的小改进，无其他行为变化

## v3.0.0

### 新功能
- **PC 桌面端完整适配（Windows / Linux）**：沉浸式自定义标题栏 + 侧边栏 + 底部播放栏的桌面布局，歌单 / 排行榜 / 歌手 / 搜索 / 已下载页全部表格化，播放页分栏适配，开箱即用的桌面级体验
- **听歌识曲**：听一段旋律即可识别歌曲并直接播放；桌面支持「麦克风 / 系统内录」双采集源（内录可直接识别电脑正在播放的声音），手机与车机走麦克风，入口在搜索页 / PC 顶栏
- **桌面歌词大改版**：逐字卡拉OK高亮、单行 / 双行错行模式、悬浮窗快捷设置菜单、QQ 音乐式锁定全穿透与悬浮解锁、深色描边提升浅底可读性
- **系统集成**：系统媒体控制（Windows SMTC / Linux MPRIS）、托盘常驻与关闭行为设置、开机自启、窗口标题跟随歌曲、下载完成通知、窗口尺寸位置记忆
- **桌面快捷键**：空格播放 / 暂停、方向键切歌与快进快退、侧栏键盘导航
- **Windows 多形态分发与应用内更新**：便携版解压即用；安装版支持应用内检查更新、下载安装（sha256 完整性校验），由 GitHub Actions 自动打包
- **移动端播放页重设计**：QQ 音乐式海报布局、下拉关闭手势、封面点击弹出速操作
- **移动端歌词页重做**：卡拉OK逐字列表 + 底部控制条，与播放页融合
- 全平台 Logo / 图标全量更新，默认配色切换为「拾音金」

### 优化
- Windows / Linux 音频后端迁移至 libmpv（media_kit），播放更稳，移除 WinRT 崩溃补丁
- 封面三级缓存：桌面内存上限放宽 + 磁盘缓存 + 解码档位量化，切页往返不再重复下载图片
- 车机模式推荐页布局与字号优化，识曲页不再全屏、保留左侧常驻播放面板
- 排行榜 / 电台离线缓存降级，断网也能浏览最近内容
- 播放器 / 控制器 / API 大文件按职责拆分重构，行为不变更、可维护性提升

### 修复
- 修复 PC 端退出卡死十余秒的问题（引擎 teardown 崩溃 + WER 拖尾）
- 修复冷启动点击歌词跳转播放弹回 0 秒、歌词定位偏差的问题
- 修复 1:1 变体音轨整轨错位及兜底偏移问题
- 移动端大量加固：下载续传 / 取消 / 并发安全、歌单全量加载代际取消、播放缓存同曲常数级 IO、队列点当前歌不重播等

## v2.5.1

### 优化
- UI 界面统一优化：统一首页/排行/电台/我的页白卡圆角视觉，车机左侧面板与推荐播放按钮改为淡雅白卡体系，无歌曲时白屏兜底
- 车机触控优化：左侧播放面板爱心按钮加大（38dp → 52/60dp），车机横屏爱心从顶栏移入控制栏左侧，方便驾驶时触控
- 首页特性卡字号统一放大，窄屏自动竖排自适应；搜索框去色块、热搜榜高度自适应滚动
- 统一排行榜与电台间距，优化电台卡片布局与播放按钮，修复 Tab 切换黑闪；热搜切榜定向滑入，排行榜头收紧对齐首页
- 猜你喜欢与电台大卡支持车机字号放大，点击卡片可查看猜你喜欢歌单
- 歌单头重排与粘性歌曲条，优化歌单按钮、圆形复选框与悬浮多选操作栏
- 去除标题中冗余歌手名前缀，统一展示纯歌名；点歌后台静默补全保留滚动位置

### 新功能
- Skia/Impeller 双渲染器变体，打包与应用内更新按渲染器分流（老 GPU 闪退用户可选 Skia 包）
- 重构随机播放为无放回洗牌队列，并统一设置页 UI 风格

### 修复
- 清零 analyze 12 条 info，清理 KA Music 残留标记并统一为时音

## v2.5.0

### 新功能
- 新增车载蓝牙歌词与 SuperLyric 系统歌词广播：通过标准 + 厂商广播（网易云/QQ/酷狗/酷我）将当前行歌词同步到车机/第三方歌词 App，SuperLyric 支持 Xposed 模块逐字显示，设置页新增“车载蓝牙歌词”开关（默认关闭，性能开销可忽略）
- 我的页面重做：折叠分组（我创建/我收藏/专辑/云盘）+ 排序（默认/按名称拼音/按歌曲数/按创建时间）+ 长按多选批量删除，2x2 快捷入口（喜欢/最近/本地/已下载）

### 优化
- 底栏重做为实色悬浮胶囊（去玻璃）：竖屏底栏改为 `白/深灰 #1E2433 + 轻阴影` 悬浮胶囊，居中集成 42dp 唱片封面 + 环形进度，长按切播/点击进播放页；平板仍用侧边栏，MiniPlayer 仅平板悬浮
- 统一所有播放栏样式：歌单/搜索/专辑/本地等页的 Mini 播放栏由 `Blur 18 + 半透` 改为与底栏一致的实色悬浮卡（16 圆角 + 描边 + 阴影），多处叠加不再离屏，车机/低端机更流畅
- 去除液态玻璃重度实现：新增 `liquid_glass_ui.dart` 实色 stub，避免 `BackdropFilter` 在列表中的 GPU 开销，不引入 `liquid_glass_easy` 依赖

### 修复
- 修复车机顶栏搜索按钮深色模式不适配（浅色半透在深色背景对比度不足）及深浅切换需退出才生效的问题：内层 Navigator 路由捕获的 `colorScheme` 改为实时 `Theme.of(context)` 获取
- 修复断网恢复后首页封面不刷新、停留缓存歌、VIP 状态/每日领取漏执行的问题：网络恢复时自动刷新首页、重试封面、刷新 VIP 并补领

## v2.4.9

### 修复
- 修复断网进入首页、恢复网络后封面图片一直不显示的问题：网络恢复时失败的封面图自动重试加载
- 修复断网时首页停留在缓存歌曲的问题：网络恢复后首页（推荐 / 排行榜 / 电台）自动刷新为线上最新数据
- 修复断网启动时 VIP 状态查不到、每日 VIP 领取被漏掉的问题：网络恢复后自动刷新 VIP 状态并补跑当日领取（当天已领过不会重复领取）

## v2.4.8

### 修复
- 修复车机端首次播放时顶部弹出媒体通知横幅：车机改走专属静默通知渠道，手机端灵动岛 / 锁屏 / 通知中心体验不变
- 修复电台只能播放一首歌的问题，现在点击电台会连续播放整批歌曲（每次进入随机换批）
- 修复排行榜「播放全部」只播放已加载的前 50 首、播完回到第一首的问题，播放中会自动加载完整榜单
- 修复部分电台封面图片不显示

## v2.4.7

### 修复
- 优化播放进度条高潮提示：由进度条下方的圆点改为轨道内部的高亮小段标记高潮起始位置，竖版与车机横版播放页统一生效
- 修复歌手页专辑封面全部不显示：上游 cover 字段仅返回文件名，改为优先使用 sizable_cover 完整 URL
- 修复歌手详情页滚动后歌曲列表从透明顶栏下方穿透：顶部改为 SliverAppBar，头部收起后顶栏切换为不透明底色并显示歌手名
- 修复 Windows 调试构建 Rust DLL 不更新：CMake 改为每次构建调用 cargo 增量编译，此前 8 月的 Rust 修复（歌手专辑、专辑歌曲参数）一直未进调试包

## v2.4.6

### 修复
- 移除播放页歌词模糊渐变效果，恢复清晰歌词显示
- 桌面歌词下一句字号跟随字体大小设置等比例放大，不再固定偏小
- 桌面歌词悬浮窗支持拖拽右下角手柄调整窗口宽度，并记住上次宽度
- 修复桌面歌词锁定后无法解锁的问题：锁定不再强制触摸穿透，点击悬浮窗锁图标即可解锁
- 缩小桌面歌词拖拽手柄图标并独立占位，不再遮挡锁定/关闭按钮
- 修复重启后直接播放上次歌曲播放不了的问题：播放器空闲时播放按钮改走完整加载流程

## v2.4.5

### 新功能
- 播放器新增「试听高潮」：一键播放歌曲高潮片段，进度条上标记高潮时间点
- 歌单/专辑详情页新增多选批量下载，自动跳过已下载歌曲并复用下载队列
- 歌单详情页打开时自动定位当前播放的歌曲
- 歌单详情页新增相似歌单推荐与分享链接导入（支持网易云/QQ 音乐歌单）
- 首页新增「新歌速递」板块
- 歌手页新增专辑浏览
- 本地音乐新增「扫描目录设置」，可勾选排除指定文件夹
- 已下载页新增「下载失败」区块，支持重新下载与移除失败记录
- 新增全局字体大小设置（标准/大/特大）
- 新增桌面快捷键：空格播放/暂停，左右方向键切歌
- 播放页歌词启用模糊渐变效果

### 修复
- 修复专辑详情页歌曲为空的问题（接口参数契约与分页上限）
- 修复桌面歌词长行被截断，改为跑马灯滚动展示
- 修复桌面歌词悬浮窗宽度随歌词长短变化，现固定窗口宽度
- 修复歌词翻译与原文同时间戳显示错乱、进度条偶发回跳

### 优化
- 优化图片缓存策略：切页/返回不再反复下载图片，减少播放卡顿
- 歌词模糊渲染按行离屏模糊，降低车机渲染开销
- 移除设置页「我的 VIP」入口（保留自动领取 VIP）

## v2.4.4

- 修复手机号验证码登录失败的问题（Rust 引擎未解析 body 参数）
- 修复 API 错误响应被吞没导致无法显示具体错误信息的问题
- 新增扫码登录失败时的错误弹窗，支持一键复制错误信息
- 新增歌单内歌曲按拼音排序，中英文混排更符合读音直觉
- 移除设置页中的本地音乐入口（Library 页已有独立入口）

## v2.4.3

- 修复 VIP 过期后播放歌曲失败的问题，现在会自动领取 VIP 并重试播放，无需手动操作
- 修复手动点击「检查更新」时误提示"已是最新版本"的问题
- 修复 Windows 桌面端进入播放页后返回闪退的问题（AXTree 竞态崩溃）
- 修复部分歌曲误报"需要 VIP 才能播放"的问题（播放地址有效时不再拦截）
- 升级 APK 签名为正式 release 签名，本地与 CI 构建签名一致，可覆盖安装
- 升级 Flutter 至 3.44.8，audio_session 至 0.2.3

## v2.4.2

- 修复 VIP 过期后播放歌曲失败的问题，现在会自动领取 VIP 并重试播放，无需手动操作
- 修复手动检查更新时误提示"已是最新版本"的问题（未绕过 24 小时自动检查节流）

## v2.4.0

- 应用名称更改为「时音」，统一全平台品牌标识
- 新增排行榜页面，支持浏览酷狗各类榜单及歌曲列表，车机横屏下自动切换为双列网格布局
- 新增播放状态持久化，重启应用后自动恢复上次播放的歌曲和进度
- 新增「我的」页面 VIP 徽章和到期时间展示，每日 VIP 领取成功后自动刷新
- 新增歌单多选模式，支持批量删除等操作
- 排行榜歌曲操作菜单新增「查看歌手」功能
- 车机模式左侧播放栏点击歌名/歌手名可直接进入播放详情页
- 优化排行榜页面排版，从网格卡片改为横向列表卡片设计，信息密度更高
- 优化 Rust 引擎内存占用，降低 IoT/车机设备内存压力
- 优化图片解码尺寸与应用暂停时缓存清理，减少后台内存占用
- 减少播放 UI 重建频率，优化播放队列内存开销
- 修复排行榜歌曲「...」按钮点击直接播放的问题，改为弹出操作菜单
- 修复排行榜播放后手机版底部 mini 播放卡片不显示的问题
- 修复搜索结果始终相同的问题（参数名不匹配）
- 修复 Windows 下中文文件名本地音乐无法播放的问题
- 修复歌单收藏类型、分页和删除相关的问题
- 修复点赞/取消点赞功能异常的问题
- 修复搜索页歌手头像无法显示的问题

## v2.3.0

- 新增车机模式（在设置-个性化中开关，开启后提供大字体与左侧常驻播放面板，支持自动检测 Android Automotive 车机设备）
- 新增可调节的播放缓存上限设置，可在设置-缓存管理中修改上限
- 新增连接蓝牙、有线耳机等新音频设备时自动恢复/开始播放的功能
- 新增基于 Android 原生 DynamicsProcessing 效果器的实时音量均衡功能，可在设置中开启以平衡歌曲音量差异
- 调整 App 更新行为，将原先在应用内下载并自动弹出安装修改为直接跳转至浏览器，由用户自主下载更新包
- 优化自定义背景图预缓存机制，彻底解决切换页面时背景闪白与 1s 左右的渲染延迟问题
- 修复车机模式下切歌歌词不刷新的问题，并移除方向盘长按暂停功能
- 修复歌词翻译悬浮开关与切换按钮无效的问题
- 优化歌词翻译对应关系，并修复和优化了其他已知问题

## v2.2.0

- 修复登录页面无法修改API URL的问题
- 修复本地音乐无法拿到音乐的问题
- 优化超宽屏幕适配
- 优化底部导航栏的UI设计
- 优化歌单详情页与云盘页顶栏滚动时内容穿透的问题
- 优化歌词页播放效果
- 新增扫码登录功能
- 修复和优化了已知问题

## v2.1.0

- 新增本地音乐检索与播放功能，支持自动检索并播放手机文件夹中的音乐和歌词
- 新增本地文件夹选择窗口，支持直接选择音乐目录，不用再手动输入路径
- 支持在未登录状态下设置 API URL 服务器地址，方便私有部署
- 新增宽屏专属侧边导航栏，大屏幕下自动由底部导航切换至侧边栏
- 优化大屏页面布局防止横向拉伸，首页、我的、设置等主要页面支持居中显示
- 优化大屏网格与双列布局，我的页面歌单支持网格显示，首页推荐歌曲支持双列并排
- 优化搜索页面，底部支持显示音乐播放控制栏
- 优化搜索历史记录即时同步，解决历史新词延迟一拍显示的问题
- 优化热搜榜单对齐与展示，修复热搜榜错位、被切掉与手势冲突的问题
- 修复定时播放中“播放完再停止”开启后再次进入设置回显为关闭的问题
- 修复平板/横屏下退出播放器详情页被强制锁定回竖屏的 Bug
- 修复本地无历史记录时进行搜索导致界面卡住、搜不出任何东西的问题

## v2.0.5

- 新增播放历史记录，"我的"页面快捷入口可查看最近播放歌曲
- 新增播放统计页面，展示累计播放次数、听歌时长、最常听歌手/歌曲 Top 10
- 新增搜索历史记录，搜索页展示历史关键词标签，支持单条删除和一键清空
- 新增缓存管理可视化，设置页可查看数据缓存、下载文件、播放缓存大小并清理
- 新增歌单排序功能（按名称、歌曲数排序）和多选批量删除
- 新增歌单分享功能，复制歌单歌曲列表到剪贴板
- 新增歌单导入功能（通过 ID 导入他人歌单）
- 新增歌词交互：双击跳转播放进度、长按复制歌词、字体大小可调
- 新增智能音质切换，播放失败时自动降级音质重试
- 新增网络请求自动重试机制，弱网下更稳定
- 优化封面图加载体验，添加 Shimmer 光泽动画占位
- 优化播放队列管理：支持单项删除和清空队列
- 修复验证码与后端接收不一致的问题
- 修复首页推荐歌单点击后数据为空的问题
- 修复歌单加载完成强制滚动回顶的问题
- 修复创建歌单页面关闭时崩溃的问题
- 修复新建歌单入口消失的问题
- 修复和优化了已知问题

## v2.0.0

- 更换App Logo
- 新增全局Toast提示，移除原来的SnackBar提示
- 新增下载和缓存音乐，优化弱网或断网情况下的播放体验
- 新增歌单信息，首页信息，个人信息等缓存功能，优化加载速度
- 新增用户云盘相关功能
- 新增自定义全局皮肤，配色等个性化功能
- 新增多平台音乐搜索播放功能，暂不支持其他平台VIP歌曲和无版权歌曲
- 优化我的页面UI/UX设计
- 优化部分性能问题
- 修复底部控制栏播放队列按钮无效的问题
- 修复和优化了已知问题

## v1.6.0

- 优化全局字体大小
- 新增桌面歌词支持
- 新增音乐播放倍速
- 新增自定义API地址的功能
- 修复和优化了已知问题

## v1.5.0

- 新增后台打断机制设置，支持阻止其他 App 打断播放
- 新增被打断后自动恢复播放功能
- 新增定时播放功能
- 新增支持歌单内搜索歌曲的功能
- 新增直接在歌单页面打开歌手页
- 首页新增专辑，移除原来的每日推荐，合并到猜你喜欢。
- 关于页面新增更新日志展示
- 部分页面UI/UX设计优化

## v1.2.0

- 新增音频均衡器，支持 7 种预设音效
- 新增低音增强功能
- 支持选择绑定在当前手机号下的账号进行登录
- 新增单色启动图标（Material You）

## v1.1.0

- 新增歌词翻译和罗马音显示
- 支持逐字歌词（KRC 格式）
- 新增歌曲评论查看功能
- 新增歌手详情页面

## v1.0.0

- 首次发布
- 支持手机号验证码登录
- 支持歌单浏览和管理
- 支持搜索歌曲
- 支持多种音质切换
- 支持后台播放和通知栏控制
