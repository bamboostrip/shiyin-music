name: just_audio_media_kit
description: package:media_kit bindings for just_audio to support Linux and Windows.
# +shiyin 后缀 = 本地 fork（补丁清单见主工程 pubspec.yaml 的依赖注释，
# 勿在此处硬编码补丁数量——它一直在涨），升级上游时勿直接覆盖本目录。
version: 2.1.0+shiyin.7
repository: https://github.com/Pato05/just_audio_media_kit

environment:
  sdk: ">=3.1.3 <4.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
  just_audio_platform_interface: ^4.5.0
  media_kit: ^1.2.0
  universal_platform: ^1.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
